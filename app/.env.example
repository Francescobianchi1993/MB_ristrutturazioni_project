# MB Ristrutturazioni — variabili d'ambiente
#
# Copia questo file in `app/.env.local` per lo sviluppo locale.
# In produzione, configura le stesse variabili in Vercel:
#   Project → Settings → Environment Variables
#
# Sono variabili VITE_* perché Vite espone solo quelle al codice client.
# La anon key è pubblica per design (la sicurezza si tiene tramite RLS lato DB).

VITE_SUPABASE_URL=https://pwidhcxyyldtlagjpjkn.supabase.co
VITE_SUPABASE_ANON_KEY=
